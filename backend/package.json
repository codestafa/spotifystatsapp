{
  "name": "spotifystatsapp",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"tsc --watch\" \"nodemon -q dist/server\"",
    "build": "npm run tsc",
    "start": "node dist/server",
    "tsc": "tsc --noEmit --emitDeclarationOnly false --experimentalDecorators"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.4",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "nodemon": "^3.0.3",
    "passport-spotify": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
